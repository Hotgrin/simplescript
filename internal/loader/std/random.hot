# std/random — random numbers for games and choices.

use go
import "math/rand"
func randomBelow(n int) int { return rand.Intn(n) }
func randomBetween(low int, high int) int { return low + rand.Intn(high-low+1) }
end go
